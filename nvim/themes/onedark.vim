" Change the onedark configuration.
" There are different options: dark, darker, cool, deep, warm, warmer

let g:onedark_config = {
      \ 'style': 'deep', 
      \}

" Enable onedark colorscheme
colorscheme onedark
